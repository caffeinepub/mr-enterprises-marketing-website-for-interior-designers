{
  "kind": "build_request",
  "title": "Resize and center the header logo image",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-4",
      "text": "Update the header logo image element at XPath `/html[1]/body[1]/div[1]/div[1]/header[1]/div[1]/div[1]/a[1]/img[1]` (implemented in `frontend/src/components/layout/SiteHeader.tsx`) to be slightly smaller and visually centered by adjusting only the `<img>` element’s Tailwind classes (do not modify the parent `<a>` or other header layout elements).",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-3"
        ],
        "quotes": [
          "make it a bit smaller and move it in center"
        ]
      },
      "acceptanceCriteria": [
        "In `frontend/src/components/layout/SiteHeader.tsx`, the logo `<img>` height is reduced from its current size (currently `h-12`) to a smaller size.",
        "The logo `<img>` is horizontally centered using only `<img>`-level styling (e.g., `block` + `mx-auto` or equivalent), without changing the surrounding header structure.",
        "No other header elements (nav, buttons, containers, or the `<a>` wrapper) are modified."
      ]
    }
  ],
  "constraints": [
    "Apply changes ONLY to the selected element: the header logo `<img>` at XPath `/html[1]/body[1]/div[1]/div[1]/header[1]/div[1]/div[1]/a[1]/img[1]`.",
    "Do not edit any files under `frontend/src/components/ui` or any other immutable paths.",
    "Keep the backend unchanged."
  ],
  "nonGoals": [
    "Redesigning the header layout or repositioning navigation/menu elements.",
    "Changing the logo image file or generating new image assets.",
    "Adding new routes, sections, or backend functionality."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  },
  "userProfileUpdate": {
    "goalsToAdd": [],
    "goalsToRemove": [],
    "preferencesToAdd": [],
    "preferencesToRemove": [],
    "miscToAdd": [],
    "miscToRemove": [],
    "fieldsToSet": {},
    "fieldsToDelete": []
  }
}